<view class="weui-cells__title">demos</view>
<view class="weui-cells weui-cells_after-title">
  <navigator wx:for="{{ lists }}" wx:key="url" url="{{ item.url }}" class="weui-cell weui-cell_access">
    <view class="weui-cell__bd">{{ item.name }}</view>
    <view class="weui-cell__ft"> > </view>
  </navigator>
</view>